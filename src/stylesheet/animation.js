export const animation = {
	show: {
		currency: 'currency-show 0.5s forwards',
	},

	keyframes: {
		currencyShow:
			'currency-show {0% {opacity: 0; transform: translateX(-50px);} 50% {opacity: 1;} 100% {transform: translateX(0);}}',
	},
}
